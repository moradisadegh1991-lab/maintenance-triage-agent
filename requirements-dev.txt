# Development / security tooling (not required to run the agent itself)

# SkillSpector: scans AI agent skills for vulnerabilities and malicious
# patterns (prompt injection, data exfiltration, dangerous code execution,
# etc.) before you install or trust them.
# https://github.com/NVIDIA/SkillSpector
# Requires Python >=3.12,<3.15 (separate from the runtime requirements.txt).
skillspector @ git+https://github.com/NVIDIA/skillspector.git
